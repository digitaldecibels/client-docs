<!--
Generated by client-docs
Last audited: {{DATE}}
Source: repository
-->

# Installation

<!-- client-docs:generated:start -->

## Requirements

| Requirement | Version | Where this came from |
| --- | --- | --- |
| {{RUNTIME}} | {{VERSION}} | {{EVIDENCE_FILE}} |

Only list versions the repository states. If a version is not declared
anywhere, say so rather than guessing one.

## Steps

1. Clone the repository.
   ```bash
   git clone {{REPO_URL_OR_REQUIRES_CONFIRMATION}}
   ```
2. Install dependencies.
   ```bash
   {{PACKAGE_MANAGER_INSTALL}}
   ```
3. {{ENVIRONMENT_SETUP}}
4. {{DATABASE_SETUP}}
5. {{BUILD_STEP}}

## Verify it worked

```bash
{{VERIFY_COMMAND}}
```

{{EXPECTED_RESULT}}

## If it does not work

| Symptom | Cause | Fix |
| --- | --- | --- |
| {{SYMPTOM}} | {{CAUSE}} | {{FIX}} |

Only include problems the repository gives evidence for: a documented gotcha, a
script that checks for something, a known platform requirement. Do not invent
troubleshooting.

<!-- client-docs:generated:end -->
